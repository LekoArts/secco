packages:
  - 'packages/*'

catalogs:
  # Can be referenced through "catalog:react"
  sindre:
    pretty-ms: 9.2.0

  # Can be referenced through "catalog:" or "catalog:default"
  default:
    tslib: 2.4.1
    is-stream: 4.0.1
